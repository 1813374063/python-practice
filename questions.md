# Python 基础摸底测试（渐进式，共4关+1个加分题）

## 规则（很重要，关系到你测试结果的真实性）

- 闭卷：不查资料、不搜答案、不用任何AI（包括我），这样才能测出真实水位
- 计时建议：第1关20分钟，第2关30分钟，第3关30分钟，第4关60-90分钟
- 一道题卡住超过15分钟，做个标记直接跳下一题，不要耗着
- 半成品也要保留，卡住时脑子里怎么想的有空可以随手记在代码注释里
- 做完之后，把每一关的代码（包括没做完的）连同报错信息一起发给我，我来批改并调整你第1阶段的节奏

---

## 第1关 语法基本功

### 1-1 病历数字游戏

打印 1 到 100 的所有数字，但：
- 遇到 3 的倍数改打印"病"
- 遇到 5 的倍数改打印"历"
- 同时是 3 和 5 的倍数打印"病历"

### 1-2 候诊人数统计

各科室今天上午的候诊人数：

```python
waiting = [12, 5, 23, 8, 16, 3, 9]
```

要求：不使用 sort() 或 sorted()，用循环找出其中的最大值和最小值，并算出平均值（保留1位小数）。

---

## 第2关 函数与数据结构

### 2-1 接诊量统计

```python
records = [
    {"科室": "内科", "接诊量": 32},
    {"科室": "外科", "接诊量": 27},
    {"科室": "内科", "接诊量": 18},
    {"科室": "儿科", "接诊量": 41},
    {"科室": "外科", "接诊量": 22},
]
```

写两个函数：
- `total_by_dept(records)`：返回每个科室的总接诊量，形如 `{"内科": 50, "外科": 49, "儿科": 41}`
- `top_dept(records)`：返回总接诊量最高的科室名称

### 2-2 日志解析

系统日志里有一行：

```python
line = "2026-09-20 08:15:32 [警告] 接口HIS01响应超时"
```

写函数 `parse_log(line)`，把它解析成字典返回：

```python
{"日期": "2026-09-20", "时间": "08:15:32", "级别": "警告", "内容": "接口HIS01响应超时"}
```

---

## 第3关 文件与异常

数据文件在本文件夹的 `data/workload.csv`（提示：open 时加上 `encoding="utf-8"`，否则中文可能乱码）。

### 3-1 计算平均工作量

读取该 CSV，计算每个科室的月平均工作量（按有效数据计算，结果取整数），按下面的格式写入 `result.txt`：

```
内科: 平均工作量 120
外科: 平均工作量 100
```

### 3-2 脏数据与容错

数据里混了一行脏数据（工作量一栏是"暂无"）。要求：
- 处理时跳过它，程序结束前打印"跳过脏数据 X 行"
- 如果数据文件不存在，程序打印"找不到数据文件，请检查路径"后正常退出，而不是直接报错崩溃

---

## 第4关 综合自测（即学习计划里第1阶段的终点题）

只用 Python 自带的 sqlite3 库，不需要安装任何东西：

1. 创建一个数据库，建一张"科室工作量"表（字段：科室、月份、工作量）
2. 插入至少 6 条模拟数据
3. 用 SQL 查出总工作量前 3 名的科室并打印
4. 把查询结果导出成 CSV 文件（如果你会 pandas 或 openpyxl，导出成 Excel 算加分）

---

## 加分题（可选，第2阶段预习）

用 requests 库调用任意一个免费公开 API，把返回的 JSON 里挑一个字段解析出来打印。做不出来完全不影响评估，纯粹提前感受一下"调接口"是怎么回事。

---

## 自评参考（做完再看）

- 第1-2关顺利：语法和基本数据结构过关，第1阶段可以压缩到1周
- 第3关卡住：正常，文件读写和异常处理本来就是你下一步要补的
- 第4关做不出：也正常，它就是第1阶段的终点线，做出来了说明可以直接进第2阶段
